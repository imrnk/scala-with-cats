package eitherxample

object DeAllocation {

  /*def process(values : List[Double]) = {
    if(values forall( d => d == 0.0)) Left(DeallocationFailure)
    else Right(values.foldLeft(0.0)(_ + _))
  }*/
}
