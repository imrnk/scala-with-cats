package eitherxample

class Result(cobDate : String, df : List[String])


object TSAggregation {


 /* def process(inputs: List[Input]) = {

    inputs.map(in => {
      if (in.reportingSetId == 4) Left(AggregationFailure(in.cobDate, in.reportingSetId))
      else Right(in.reportingSetId, in.value)
    })
  }*/


}
